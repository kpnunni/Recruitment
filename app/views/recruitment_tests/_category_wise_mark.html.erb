
<div class="table">
  <div class="tr">
    <div class="th">Category</div>
    <div class="th">no of questions</div>
    <div class="th">Right answers</div>
    <div class="th">Wrong answers</div>
    <div class="th">Mark</div>
    <div class="th">Mark(%)</div>
  </div>
  <% @categories.each do |cat| %>
      <%  no_of_questions = @recruitment_test.candidate.schedule.exam.questions.select { |q| q.category_id == cat.id}.size  %>
      <% if no_of_questions > 0 %>
          <div class="tr">
            <div class="td"><%=cat.category    %></div>
            <div class="td"><%= no_of_questions %></div>
            <div class="td"><%= @recruitment_test.each_right_answers(cat) %></div>
            <div class="td"><%= @recruitment_test.each_wrong_answers(cat) %></div>
            <div class="td"><%= @recruitment_test.calc_mark(cat)  %></div>
            <div class="td"><%= number_with_precision(@recruitment_test.calc_mark_percentage(cat),:precision=>2)  %></div>
          </div>
      <% end %>
  <% end %>
</div>

<% if @extra_answers.present? %>
    <br/>
    <h3>Additional answers:</h3>

    <div class="table"  >
      <div class="tr">
        <div class="th">Question</div>
        <div class="th">Answer</div>
        <div class="th">Right answers</div>
        <div class="th">Right?</div>
        <div class="th">Complexity</div>
        <div class="th">Time Taken(sec)</div>
      </div>
      <% @extra_answers.each do |ans| %>
          <% if ans.question %>
              <div class="tr">
                <div class="td lft"><%= link_to   ans.question.question.slice(0..50), "#", remote: true, :onclick => "javascript:window.open(\"#{question_path(ans.question)}\",'popup','width=600,height=600');"   %></div>
                <div class="td"><%= ans.answer %></div>
                <div class="td"><%= ans.question.answer_id %></div>
                <div class="td" style="background-color: <%= ans.answer == ans.question.answer_id ? "#2e8b57" : "#cd5c5c"  %>;"><%= ans.answer == ans.question.answer_id ? "Yes" : "No" %></div>
                <div class="td"><%= ans.question.complexity.complexity %></div>
                <div class="td"><%= ans.time_taken %></div>
              </div>
          <% end %>
      <% end %>
    </div>
<% end %>

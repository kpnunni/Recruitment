
New Schedule detail


      Scheduled date is: <%= @schedule.sh_date.strftime("%b %d %A,%Y") %>.
      Scheduled time is: <%= @schedule.sh_time.strftime("%I:%M%p") %>.
      Scheduled test is: <%= @schedule.exam.name %>.
      Candidates are:
      <% @schedule.candidates.each {|c|  %>
       <%= c.name%>  ,
     <% } %>


      To login to the site, just follow this link: <%= @url %>.


